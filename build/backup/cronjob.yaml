apiVersion: batch/v1beta1
kind: CronJob
metadata:
  name: oc-backup
spec:
  schedule: "33 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: oc-backup
            image: 
            args:
            - /bin/sh
            - -c
            - date; echo Hello from the Kubernetes cluster
          restartPolicy: OnFailure
